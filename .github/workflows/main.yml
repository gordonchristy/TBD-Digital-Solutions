name: Hello World Workflow
on: [push] # Runs every time you push code

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo "Hello, world! Your workflow is running successfully."
